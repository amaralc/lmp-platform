// IMPORTS ---------------------------------------------------------------------
const app = require('./app');

// CONTENT ---------------------------------------------------------------------
app.listen(3333);